name: Kalpixk CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  RUST_BACKTRACE: 1

jobs:
  rust-check:
    name: "Rust — clippy + tests"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Instalar Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
          targets: wasm32-unknown-unknown

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Clippy (warnings, no fail)
        run: cargo clippy --all-targets -- -D warnings
        continue-on-error: true

      - name: Tests unitarios
        run: cargo test --all

  build-wasm:
    name: "WASM — wasm-pack build"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Instalar Rust + wasm32 target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Instalar wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: wasm-pack build
        run: |
          cd crates/kalpixk-core
          wasm-pack build --target web --release
          echo "WASM: $(du -sh pkg/kalpixk_core_bg.wasm | cut -f1)"
          ls -lh pkg/

      - name: Upload WASM artifact
        uses: actions/upload-artifact@v4
        with:
          name: kalpixk-wasm
          path: crates/kalpixk-core/pkg/
          retention-days: 7

  python-check:
    name: "Python — pytest sin GPU"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          pip install --quiet             fastapi "uvicorn[standard]" pydantic             numpy scikit-learn msgpack websockets             httpx pytest pytest-asyncio
          pip install --quiet torch --index-url https://download.pytorch.org/whl/cpu

      - name: pytest
        run: |
          cd python
          python -m pytest tests/ -v --tb=short --ignore=tests/test_api.py
        env:
          KALPIXK_FORCE_CPU: "true"
        continue-on-error: true

  frontend:
    name: "Frontend — Vite build"
    runs-on: ubuntu-latest
    needs: build-wasm
    steps:
      - uses: actions/checkout@v4

      - name: Descargar WASM artifact
        uses: actions/download-artifact@v4
        with:
          name: kalpixk-wasm
          path: web/src/wasm/

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: npm install + build
        run: |
          cd web
          npm install
          npm run build
        env:
          VITE_BASE_URL: /Wasm-Kalpixk_IA_DevOps/

      - name: Verificar build
        run: find web/dist -type f | head -20
